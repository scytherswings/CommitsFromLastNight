<%= simple_form_for(@repository) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :owner %>
    <%= f.input :first_commit_sha %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
