<div class="page-header">
  <%= link_to filter_categories_path, class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <%= link_to edit_filter_category_path(@filter_category), class: 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-pencil"></span>
    Edit
  <% end %>
  <h1>Show filter_category</h1>
</div>

<dl class="dl-horizontal">
  <dt>Category:</dt>
  <dd><%= @filter_category.category_id %></dd>

  <dt>Category:</dt>
  <dd><%= @filter_category.category_id %></dd>

  <dt>Filterset:</dt>
  <dd><%= @filter_category.filterset_id %></dd>

  <dt>Filterset:</dt>
  <dd><%= @filter_category.filterset_id %></dd>

</dl>
