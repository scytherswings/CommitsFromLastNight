<%= simple_form_for(@filter_category) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :category_id %>
    <%= f.input :category_id %>
    <%= f.input :filterset_id %>
    <%= f.input :filterset_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
